# use-moose

**Workflow skill** — one narrow path: **new project** from the Moose **`typescript-agent`** harness template + template **Quickstart** only.

Edit [`SKILL.md`](./SKILL.md) only. No build step.

Canonical upstream steps: Moose tutorial *Production-Ready Analytics Agent* (Get Running Locally + template README Quickstart), reachable via `moose docs guides/production-ready-analytics-agent/tutorial --lang ts`.
